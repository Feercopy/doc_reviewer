import { AppShell } from "@/components/AppShell";

export default function BenchmarksPage() {
  return (
    <AppShell>
      <main className="main stack">
        <h1>Benchmarks</h1>
        <section className="panel muted">Benchmark runs are planned for phase 4.</section>
      </main>
    </AppShell>
  );
}
