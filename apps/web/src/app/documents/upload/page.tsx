import { AppShell } from "@/components/AppShell";

export default function UploadPage() {
  return (
    <AppShell>
      <main className="main stack">
        <h1>Upload</h1>
        <section className="panel muted">Upload workflow is planned for phase 2.</section>
      </main>
    </AppShell>
  );
}
