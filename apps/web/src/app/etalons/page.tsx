import { AppShell } from "@/components/AppShell";

export default function EtalonsPage() {
  return (
    <AppShell>
      <main className="main stack">
        <h1>Etalons</h1>
        <section className="panel muted">Etalon workflow is planned for phase 4.</section>
      </main>
    </AppShell>
  );
}
