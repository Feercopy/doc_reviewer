import { AppShell } from "@/components/AppShell";

export default function SettingsPage() {
  return (
    <AppShell>
      <main className="main stack">
        <h1>Settings</h1>
        <section className="panel muted">Provider key settings are planned for phase 3.</section>
      </main>
    </AppShell>
  );
}
